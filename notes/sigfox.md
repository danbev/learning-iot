## Sigfox
Like LoRa this is an Low-power WAN which uses unlicenced frequency (unlike
nb-iot).

It does not support two-way communication so it cannot acknowledge messages and
this makes them more efficient where such functionality is not required.


TODO: take a closer look at how SigFox works.
